/**
 * Declare duas variáveis;
 * variável 1 = nome
 * variável 2 = sobrenome
 * Crie uma terceira variável
 * Atribua a concatenação de nome + sobrenome à terceira var
 * var 3 = nomeCompleto
 * Imprima
 */

let nome = "Baker",
  sobrenome = "Mayfield",
  espaco = " ",
  nomeCompleto;

nomeCompleto = nome + espaco + sobrenome;
console.log(nomeCompleto);
