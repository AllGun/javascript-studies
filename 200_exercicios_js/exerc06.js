/**
 * Exercício 6:
 * Declare duas variáveis
 * Atribua valores booleanos a elas
 * Use osoperadores lógicos AND, OR e NOT para realizar operações lógicas entre elas
 * Imprima os resultados
 *
 *
 * Battisti, Matheus. 200 Exercícios de JavaScript: Fortaleça suas habilidades com desafios: Domine JavaScript com exercícios interativos (Portuguese Edition) (p. 18). Edição do Kindle.
 */

let bool1 = true,
  bool2 = false;

let and = bool1 && bool2;
console.log(and);

let or = bool1 || bool2;
console.log(or);

let not = !bool1 && bool1;
