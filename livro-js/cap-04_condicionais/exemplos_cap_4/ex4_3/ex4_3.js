let idade = 10;

const firstCategory = idade >= 18 ? "Adulto" : "Juvenil";

console.log(firstCategory);

let secondCategory;

if (idade >= 18) {
  secondCategory = "Adulto";
} else {
  secondCategory = "Juvenil";
}
